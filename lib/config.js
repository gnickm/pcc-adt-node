// --------------------------------------------------------------------------
// lib/config.js
// --------------------------------------------------------------------------

var nconf = require('nconf');

//--------------------------------------------------------------------------

nconf.env().argv();

nconf.file('config.json');

nconf.defaults({
	logger: {
		level: 'debug',
		file : 'none',
	},
});

module.exports = nconf;
